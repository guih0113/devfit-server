{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["knowledge/**", "src/docs/**"]
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "api/index.ts" }
  ]
}